{
  "name": "@continue/mcp-client",
  "version": "1.0.0",
  "description": "Continue MCP Client",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "continue-mcp-client": "./dist/cli.js"
  },
  "scripts": {
    "build": "esbuild src/index.ts src/cli.ts --outdir=dist --bundle --platform=node --format=cjs --external:@modelcontextprotocol/sdk",
    "test": "jest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "esbuild": "^0.24.2",
    "jest": "^29.7.0",
    "typescript": "^5.6.3"
  }
}
